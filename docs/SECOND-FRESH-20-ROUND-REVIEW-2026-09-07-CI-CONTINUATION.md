# File 26 — Round 1 CI Continuation Ledger — 2026-09-07

Parent ledger: `docs/SECOND-FRESH-20-ROUND-REVIEW-2026-09-07.md`
Branch: `review/file26-second-fresh-20-round-2026-09-07`
Baseline HEAD: `9067d19a82eeae59713a705cd16f3e56a3ec435e`

This file continues the frozen Round-1 regression-gate findings after R1-D15. The same governing sequence applies: **evidence → freeze finding → correction → exact-head CI**. Round 2 remains prohibited until Round 1 exact-head CI is green.

- **R1-D16 — High — Round-20 interpolation scanner falsely classifies escaped or single-quoted literals as executable interpolation:** exact-head run `34087807240` on `8dc844f08c60448e3f39c2b4d6d89dd55a664c80` passed every sequential review regression through both Round-19 gates, then `tests/review-round-20-release-evidence.php` rejected four already-passing regression files as “interpolation-prone.” Inspection proves the flagged `$document`, `$manifest`, and `$this` occurrences are explicitly escaped inside double-quoted PHP strings, while the `$audience` occurrence is inside a single-quoted regex literal. Those forms do not interpolate at runtime. The scanner regex does not distinguish escaped dollars or PHP single-quoted literals and therefore produces false positives against safe test code.

- **R1-D17 — High — Round-24 search eligibility/index atomicity regression is source-format coupled:** exact-head run `34087942821` on `6402fa84f9a65104764f1dc91672c8ecb7f1957e` passed all sequential regression gates through Round 23, then failed on the first Round-24 safeguard with `Missing search safeguard: 'audience' => $audience_fingerprint`. Inspection of `class-file26-search.php` confirms the cursor context includes the audience fingerprint as `'audience'=>$audience_fingerprint`, so cursor validity is bound to the current eligibility context. The regression compares a spaced source literal and exits on first mismatch. The same test also contains additional formatting-sensitive source needles across Search, REST and Indexer; the whole Round-24 regression must be normalized semantically rather than repaired one literal at a time.

- **R1-D18 — High — Round-25 connector-registry regression is formatting-coupled:** completion review of the remaining sequential gates found `tests/review-round-25-connector-registry-truth.php` still asserts spaced source shapes such as `array_intersect_key( $manifest, array_flip( $public_keys ) )` and `if ( is_wp_error( $persisted ) )`. Current `class-file26-connectors.php` implements the required public metadata allowlist, persist-result error propagation, registry read/write failures, health-write failure evidence and degraded registry-read truth in compact form. The regression therefore risks another false negative solely from formatting and must normalize source before comparison.

- **R1-D19 — High — Round-26 taxonomy atomicity regression contains a format-sensitive alias-collision read assertion:** full review of `tests/review-round-26-taxonomy-atomicity.php` against `class-file26-taxonomy.php` confirms the taxonomy runtime has atomic term creation, parent-current validation, merge/split preview failure truth, alias cleanup failure truth, classification target read failure truth, and explicit alias collision reads before insert/retry. However the regression asserts the spaced source fragment `SELECT term_uuid FROM ' . DB::table( 'term_aliases' )` while the runtime uses the equivalent compact concatenation `SELECT term_uuid FROM '.DB::table('term_aliases').'`. The test must normalize source so it verifies behavior rather than formatting.

- **R1-D20 — Medium — Round-24 regression requires a non-executable prose comment that is absent while the safety invariant is enforced by code:** exact-head run `34088261630` on `bb4e2310f0d1a1e2b623c945c89a39e5183bdcbe` passed every sequential regression through Round 23 and the corrected audience/cursor check, then failed only the Round-24 assertion labelled “signed-delivery exclusion is documented at the index boundary.” The runtime `Indexer::sanitize_payload()` uses an explicit allowlist that excludes `download_url`; the same regression separately rejects any `download_url` allowlist entry and verifies legacy `JSON_REMOVE(payload,'$.download_url')` cleanup. Requiring a specific prose sentence adds no executable safety evidence and creates a false negative when comments change. The correction must retain the executable allowlist/removal checks and drop the commentary-only requirement.

- **R1-D21 — Medium — Round-25 regression emits an undefined-variable notice from its own blacklist literal:** exact-head run `34088433799` on `eb7db274d861bcd512275f04b7dedf90b320e01c` passes sequential regressions through Round 24. Round 25 itself reports PASS, but PHP emits `Undefined variable: private_key` because the forbidden source fragment is stored in a double-quoted PHP string containing `$private_key`. This is test-code interpolation, not a runtime connector defect, but a clean regression gate must not emit notices or depend on accidental interpolation. The literal must be made non-interpolating while preserving the blacklist-detection assertion.

- **R1-D22 — High — Round-27 graph regression corrupts its own expected safeguard by interpolating `$updated`:** the same exact-head run reaches Round 27 after Round 25 and Round 26 pass, then emits `Undefined variable: updated` and fails with the malformed expected fragment `if ( false ===  ) { return new \\WP_Error( 'file26_edge_write_failed'`. The production graph code is not yet disproved by this failure; the regression string itself removed `$updated` during PHP interpolation. The expected literal must be made non-interpolating and the graph safeguard re-tested on the exact resulting HEAD.

R1-D16 through R1-D22 were frozen before their corrections.
